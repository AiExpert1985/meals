import 'package:flutter/material.dart';

class LeftDrawer extends StatelessWidget{
  const LeftDrawer({super.key});

  @override
  Widget build(Object context) {
    return const Drawer(child: Text('sdfsdsdf'),);
  }

}